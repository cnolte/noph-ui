// Component tests render the components on their own, so the theme they read their colours,
// shapes and motion tokens from has to be loaded here the way an app loads it.
import '#lib/themes/defaultTheme.css'
